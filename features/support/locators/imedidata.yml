Imedidata:
  Sign In:
    username:
      label: 'Username'
      input: '#session_username'
    password:
      label: 'Password'
      input: '#session_password'
    submit:
      text: 'Log in'
      button: '#create_session_link'
  Header:
    section:
      panel: '#header'
    signout:
      link: '#user a[data-toggle="dropdown"]'
      dropdown: '#user-dropdown'
    logout:
      link: 'li.logout #logout'
